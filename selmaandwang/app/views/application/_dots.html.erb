<script type="text/paperscript" canvas="myCanvas">
// settinig up the new firebase
  var myDataRef = new Firebase('https://selmaandwang.firebaseio.com/'); 
   var user;
   var color1;
   var color2;
   var color3;
   var color4;
   // loading the firebase assets
   myDataRef.on("value",function(snapshot) {
     var a = snapshot.val();
     for (var key in a) { 
       var obj = a[key];
       whoa(obj.x, obj.y, obj.color)
     }
   });
   // function for drawing the dots on the page
   function whoa(x,y,color) {
        var point = new Point(x,y)
     var myCircle = new Path.Ellipse({
       size: [15,20] ,
       center: point,
       fillColor: color 
     });
       view.update();
   }
   // function the pushes the coordinates to firebase
   function onKeyDown(event) {
    if (event.key == 'space') {
     var x = Math.floor((Math.random() * 1400) + 100);
     var y = Math.floor((Math.random() * 800) + 100);
     var a = Math.floor((Math.random() * 4) + 1);
       if (a == 1) {
       var color  = 'red';
     } else if (a == 2) {
         var color= 'blue';
     } else if (a == 3) {
        var color = 'peachpuff';
     } else if (a == 4) {
       var color  = 'tomato';
     }
     myDataRef.push({ x: x, y: y, color: color})
     console.log(x,y)
   
    }

   }
   // function that asks the question on mouse click(unused)
   // function onMouseDown(event) {
   //   ask();
   // }
 </script>
